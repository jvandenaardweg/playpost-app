import { StyleSheet } from 'react-native';
import spacing from '../../constants/spacing';

export default StyleSheet.create({
  container: {
    height: 40,
    width: spacing.default + 40,
    alignItems: 'center',
    flex: 1,
    flexDirection: 'row',
    paddingLeft: spacing.default
  },
  icon: {

  }
});
