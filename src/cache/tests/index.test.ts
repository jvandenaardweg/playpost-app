// import { getLocalFilePath } from '../index';

describe('cache', () => {
  describe('getLocalFilePath', () => {
    // it('should return the local file path', () => {
    //   expect(getLocalFilePath('a/path/to/something.mp3', 'base')).toEqual('file://base/something.mp3');
    // });

    it('test', () => {
      expect('test').toEqual('test');
    });
  });
});
