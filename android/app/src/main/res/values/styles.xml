<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- <item name="android:textColor">#000000</item> -->
        <item name="colorAccent">#037DE2</item>
    </style>

    <!-- <style name="Share.Window" parent="android:Theme"> -->
    <style name="Share.Window" parent="Theme.AppCompat">
        <item name="android:windowEnterAnimation">@null</item>
        <item name="android:windowExitAnimation">@null</item>
    </style>

    <!-- <style name="Theme.Share.Transparent" parent="android:Theme"> -->
    <style name="Theme.Share.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">false</item> <!-- Set this to false should fix a crash on Android versions below 28 https://stackoverflow.com/a/52964692/3194288 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/Share.Window</item>
    </style>

</resources>
